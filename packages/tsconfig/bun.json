{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "@repo/tsconfig — bun (Hono API / server)",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ESNext"],
    "types": ["bun"],
    "allowImportingTsExtensions": true,
    "noEmit": true
  }
}