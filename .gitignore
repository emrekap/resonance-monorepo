# Dependencies
node_modules/

# Build outputs
dist/
build/
out/
.next/
.expo/
*.tsbuildinfo

# Turborepo
.turbo/

# Env / secrets
.env
.env.*
!.env.example

# Python (apps/ml)
__pycache__/
*.py[cod]
.venv/
venv/

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# Brainstorming mockups (superpowers visual companion)
.superpowers/

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Terraform (infra/deploy/terraform) — .terraform.lock.hcl IS committed (pins
# provider versions), everything else here is local state or real credentials.
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example

# NOTE: commit the Bun lockfile (bun.lock / bun.lockb) — do not ignore it.