# apps/mobile

**Expo / React Native** app — the primary Resonance client.

Consumes the Bun API via the Hono RPC client (`hc<AppType>` from [`@repo/api-contract`](../../packages/api-contract)),
wrapped in TanStack Query. No codegen — typesafety comes from importing the API's `AppType`.

_Scaffold placeholder — `npx create-expo-app` goes here._