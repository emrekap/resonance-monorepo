{
  "name": "@repo/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/db": "*",
    "@repo/queue": "*",
    "bullmq": "^6.0.5"
  },
  "devDependencies": {
    "@repo/tsconfig": "*",
    "@types/bun": "^1.3.14",
    "typescript": "^5.6.0"
  }
}
