# apps/web

**Next.js** web app (later — after mobile).

Same typesafety path as mobile: consumes the Bun API via the Hono RPC client (`hc<AppType>` from
[`@repo/api-contract`](../../packages/api-contract)) + TanStack Query.

_Scaffold placeholder._
